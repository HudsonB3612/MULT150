/*
For the method:
    int TakeDamage(int damageAmount)
    {
        int health = 100;
        return health - damageAmount;
    }

1. What is the method’s name?
   - The method's name is TakeDamage

2. What variable type does the method return?
   - It returns an int

3. What are the method's parameters? How many are there?
   - There is one parameter which is an integer named damageAmount

4. What code is in the method’s block?
   - The block declares an int variable named health and initializes it to 100
   - It then returns the result of subtracting damageAmount from health
*/
